export function tLocationTwint(location) {
  // TODO: replace with real numbers from your PDF when you want.
  if (location === 'corbieres') return { label: 'Corbières', number: '+41 XX XXX XX XX' }
  return { label: 'AZH Romont', number: '+41 XX XXX XX XX' }
}
